#include <stdio.h>

typedef struct	s_node
{
	struct node *next;
	int x;
	int y;
}t_node;

int	main(void)
{
	int index = 0;
	int i = 0;
	
	scanf("%d", &index);
	getchar();
	while (i < index)
	{
		scanf("%d %d", );
		getchar();
	}
	
	return (0);
}
